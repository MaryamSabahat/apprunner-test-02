version: 1.0
runtime: python3
build:
  commands:
    build:
      - python -m pip install --upgrade pip
      - python -m pip install -r requirements.txt
run:
  command: python app.py
  network:
    port: 8080
